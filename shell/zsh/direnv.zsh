# NOTE: I'm currently running a fork of direnv to allow persisting "deny".
#
# https://github.com/direnv/direnv/pull/400
# https://github.com/kalbasit/direnv/tree/direnv_persist-deny

eval "$(direnv hook zsh)"
